import tkinter as tk

# Placeholder de la Fase 4: solo existe para validar que el pipeline de
# PyInstaller compila y publica correctamente en R2. La reescritura completa
# (sin token hardcodeado, botones de refresh/self-update, validación de
# carpeta, estética nueva) es la Fase 5.

if __name__ == "__main__":
    root = tk.Tk()
    root.title("RE4MP Updater")
    root.geometry("300x120")
    tk.Label(root, text="re4mp_updater\n(placeholder - Fase 5 pendiente)").pack(expand=True)
    root.mainloop()
